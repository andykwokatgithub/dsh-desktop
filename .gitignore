# Build artifacts
/dsh-desktop.exe
*.exe
*.exe~
*~
*.dll

# Go workspace-local module/build cache (kept inside the repo for sandboxed builds)
/.gopath/

# Editor/OS
.DS_Store
Thumbs.db
# dsh-error-knowledge: keep raw error context (may contain secrets) out of git
.dsh/error-knowledge/
